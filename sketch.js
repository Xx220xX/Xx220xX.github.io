// to run ctrl alt l
  // to stop ctrl alt q
function setup() {
  // put setup code here
  createCanvas(500,500)
  background(255,0,0)
  let s = 'The quick brown fox jumped over the lazy dog.';
  fill(50);
  text(s, 10, 10, 70, 80);
}

function draw() {
  // put drawing code here
}
